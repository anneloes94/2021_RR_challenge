import React from "react";
import Order from './Order'

export default function Driver() {
  return (
    // if no orders, display:
    // "No Orders Assigned"
    // else, for each order create Order component:
    <div>Hello I am a Driver Component</div>
  )
}